#ifndef BITTER_BIT_ORDER_HPP
#define BITTER_BIT_ORDER_HPP

namespace bitter {

enum class bit_order
{
    msb0, // most significant bit is bit 0
    lsb0 // least significant bit is bit 0
};

} // namespace bitter

#endif // BITTER_BIT_ORDER_HPP
